import "@nomicfoundation/hardhat-toolbox";

export default {
  solidity: "0.8.28",
  networks: {
    sepolia: {
      url: process.env.QUICKNODE_URL,
      accounts: [process.env.PRIVATE_KEY]
    }
  }
};